# ALIAS_LINUX

alias keypub="cat ~/.ssh/id_rsa.pub"

# commands
alias load="source ~/.bashrc"
alias del="rm -rf"

# dirs
alias c="cd .."
alias cd..="cd .."

alias proxy="env | grep proxy"
# ALIAS_LINUX END
