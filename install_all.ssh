#installation from scratch

#REPOS
VIM_NAME_DIR="vim"
VIM_REPO="https://github.com/vim/vim.git"

TMUX_NAME_DIR="tmux"
TMUX_REPO="https://github.com/tmux/tmux.git"

#INSTALLATION_CONFIGS
BASE_DIR="/home/$USER/"
MAIN_DIR=$BASE_DIR"dotFiles"

mkdir $MAIN_DIR
cd $MAIN_DIR

git clone $VIM_REPO $VIM_NAME_DIR
git clone $TMUX_REPO $TMUX_REPO

